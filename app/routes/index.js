module.exports = (app) => {
    /**
     * @param {string} FEATURE_FOLDERNAME depends on module name
     * @param {string} FEATURE_FILENAME depends on module name 
     */
    // require('./{FEATURE_FOLDERNAME}/{FEATURE_FILENAME}.routes')(app)
}